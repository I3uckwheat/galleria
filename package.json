{
  "name": "gallery",
  "version": "0.1.0",
  "description": "Generates a gallery from images, handles uploading.",
  "main": "gallery.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jimp": "^0.2.28",
    "multer": "^1.3.0"
  }
}
